#include <stdio.h>
#include <string.h>

void print_string(char * str, int s, int k, int u){
    
    if(s+k+u==0){
        printf("%s",str);
    }
    else{
        for(int i=0; i<3; i++){
            if(left_num)
        }
    }
}

int main(){
    int s,k,u;
    scanf("%d %d %d", &s,&k,&u);


    return 0;
}